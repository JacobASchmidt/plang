module main

import (
	"http"
)

def main() 
    app = http.NewApp()
        .Get("/",      (req, resp) => http.Static(req, resp, http.Dir("static")))
        .Get("/hello", (req, resp) => resp.Write("Hello, World!" to [byte]))
        .ListenAndServe(":8080")
    